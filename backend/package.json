{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "tsx --watch ./src",
    "migrate:make": "knex migrate:make --migrations-directory ./src/database/migrations -x ts",
    "migrate:up": "knex migrate:up --knexfile ./src/database/knexfile.ts",
    "migrate:latest": "knex migrate:latest --knexfile ./src/database/knexfile.ts",
    "migrate:rollback": "knex migrate:rollback --knexfile ./src/database/knexfile.ts",
    "migrate:list": "knex migrate:list --knexfile ./src/database/knexfile.ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-session": "^1.17.3",
    "knex": "^3.0.1",
    "passport": "^0.6.0",
    "passport-local": "^1.0.0",
    "pg": "^8.11.3"
  },
  "devDependencies": {
    "@types/express": "^4.17.19",
    "@types/node": "^20.8.6",
    "ts-node": "^10.9.1",
    "tsx": "^3.13.0",
    "typescript": "^5.2.2"
  }
}
