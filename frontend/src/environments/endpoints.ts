import {JsonObject} from "type-fest";

export const endpoints: JsonObject = {
    signup: "auth/signup",
    login: "auth/login",
    logout: "auth/logout",
    isAuth: "auth/is-auth",
    users: "api/users"
}
